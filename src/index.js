// files `package.json` and `src/index.js` are not created by `Coder-boot-web`.
function myFunction() {
    // Core functionality of the package
    return "Hello, World!";
}

module.exports = myFunction;
